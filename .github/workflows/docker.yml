name: dealers-choice docker image
concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.sha }}
  cancel-in-progress: true

on:
  workflow_dispatch:
  push:
    branches: trunk

env:
  GHCR_REGISTRY_IMAGE: ghcr.io/dealer-s-choice/dealers_choice
  VERSION: latest

permissions:
  contents: read
  packages: write

jobs:
  build:
    runs-on: ${{ matrix.os }}
    name: ${{ matrix.codename }}-${{ matrix.os }}-image
    strategy:
      matrix:
        os:
          - ubuntu-24.04-arm
          - ubuntu-24.04

      fail-fast: true
    steps:
      - name: Prepare
        run: |
          platform=$(uname -m)
          echo "PLATFORM_PAIR=${platform//\//-}" >> $GITHUB_ENV
      - name: Checkout
        uses: actions/checkout@v4
      - name: Docker meta
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: |
            ${{ env.GHCR_REGISTRY_IMAGE }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push by digest
        id: build
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./docker/Dockerfile.alpine
          labels: ${{ steps.meta.outputs.labels }}
          outputs: type=image,"name=${{ env.GHCR_REGISTRY_IMAGE }}",push-by-digest=true,name-canonical=true,push=true
          cache-from: type=registry,ref=${{ env.GHCR_REGISTRY_IMAGE }}:alpine-${{ env.PLATFORM_PAIR }}-${{ env.VERSION }}-buildcache
          cache-to: type=registry,ref=${{ env.GHCR_REGISTRY_IMAGE }}:alpine-${{ env.PLATFORM_PAIR }}-${{ env.VERSION }}-buildcache,mode=max


      - name: Export digest
        run: |
          mkdir -p /tmp/digests
          digest="${{ steps.build.outputs.digest }}"
          touch "/tmp/digests/${digest#sha256:}"
      - name: Upload digest
        uses: actions/upload-artifact@v4
        with:
          name: digests-alpine-${{ env.PLATFORM_PAIR }}
          path: /tmp/digests/*
          if-no-files-found: error
          retention-days: 1

  merge:
    runs-on: ubuntu-latest
    needs:
      - build
    steps:
      - name: Download digests
        uses: actions/download-artifact@v4
        with:
          path: /tmp/digests
          pattern: digests-alpine-*
          merge-multiple: true

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Docker meta
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: |
            ${{ env.GHCR_REGISTRY_IMAGE }}
          tags: |
            type=raw,value=${{ env.VERSION }}-alpine

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Create manifest list and push
        working-directory: /tmp/digests
        run: |
          for build in ${GHCR_REGISTRY_IMAGE}; do
            docker buildx imagetools create $(jq -cr '.tags | map("-t " + .) | join(" ")' <<< "$DOCKER_METADATA_OUTPUT_JSON") \
              $(printf "${build}@sha256:%s " *)
          done

      - name: Inspect image
        run: |
          for build in ${GHCR_REGISTRY_IMAGE}; do
            docker buildx imagetools inspect ${build}:${{ steps.meta.outputs.version }}
          done
