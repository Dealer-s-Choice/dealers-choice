bind_address = NULL
port = 22777
end_of_game_timeout = 15
action_timeout = 30
action_timeout_max = 3
dealer_timeout = 30
ante = 50
bringin_amount = 50
starting_coins = 20000
max_raises = 3
bet_amounts = list, 100, 250, 500
password =
max_connections_per_minute = 10
max_connections_per_ip = 0

# Name shown to players (LAN discovery + the registry).
server_name = Dealer's Choice server

# Publish this server to one or more registries (directory servers) so internet
# players can find it. Comma-separated host or host:port (default registry port
# is 22070). Leave unset, or pass --disable-publish, to not publish.
# registry = registry.example.org, 203.0.113.5:22070
